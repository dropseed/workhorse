pulls:
  search: "is:open org:dropseed"
  filter:
    mergeable: true
  markdown: "{{.HTMLURL}} - {{.Title}}"
  steps:
    - add_labels:
        labels: [automerge]
    - remove_label:
        label: automerge
    - close: {}
    - sleep:
        duration: 2.5
