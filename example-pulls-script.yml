pulls:
  search: "is:open org:dropseed"
  filter:
    mergeable: true
  steps:
    - add_labels:
        labels: [automerge]
    - remove_label:
        label: automerge
    - sleep:
        duration: 2.5
